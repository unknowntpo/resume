# Cyberon — AI 平台開發工程師求職信

您好：

我想應徵 AI 平台開發工程師。這個職位吸引我的地方，是團隊希望建立可重複使用的企業 AI 方法與平台，而不是為每個場景堆疊一次性的模型串接。

我目前獨立開發開源專案 Locus：一個 provider-neutral 的 real-world context federation layer，讓 AI agents 能共同使用不同領域與供應商的資料。它不是把多個 API 集中起來，而是以 identity、location、freshness、expiry、provenance、confidence、coverage gaps、partial failures 與 conflicting observations 等共同 contract 聯邦不同資料語意，同時用 adapters 保留 provider 差異。第一個 reference flow 已將停車空位與相容 EV 充電樁狀態合併成單一決策流程，並透過 MCP、Cloudflare Workers / R2 交付。這套設計的目標正是把一次性整合抽象成可跨 domain 擴充、可驗證的 context layer。

在 production engineering 方面，我曾建立資料缺漏監控、LLM 法規文字結構化服務與政府資料更新流程，也有 PostgreSQL、Docker、Kubernetes、Linux 及雲端部署經驗。我直接參與 Apache Kafka upstream，處理 offset commit correctness、JVM container resource detection 與 Kafka Streams bounded buffering，因此對 failure semantics、資源限制、監控與可維運性有實際經驗。

我的主要強項是 Backend 與 Data，但能使用 JavaScript、Python、Go、Java 與 Rust，並習慣在不完整需求下先定義 contract、交付最小可行版本，再依真實限制迭代。我期待將這些能力投入企業 Agent 平台，協助團隊把不同客戶與場景中的共通需求收斂成長期可維護的產品能力。

謝謝您撥空閱讀，期待有機會進一步交流。

Eric（Chen-Chien）Chang
