# 線上教育平台 — 資深後端軟體工程師（Python／AI 應用）求職信

您好：

我想應徵資深後端軟體工程師（Python／AI 應用）。我的核心能力是把 prototype 或資料流程整理成穩定、可觀測且能持續迭代的產品後端，並在效能、資料品質與維運成本之間做出可落地的取捨。

我目前獨立開發開源專案 Locus：一個讓 AI agents 能共同使用不同領域、不同供應商真實資料的 context federation layer。它不是把多個 API 包在一起，而是透過 provider-neutral contract 與 adapters，將語意不一致的來源聯邦成共同 context，同時保留 freshness、provenance、confidence、coverage gaps、conflicts 與 partial failures。第一個 reference flow 已結合停車空位與相容 EV 充電樁狀態，並透過 MCP 與 Cloudflare Workers / R2 交付給使用者。這段經驗涵蓋 AI tool design、API、資料模型、部署，以及將跨來源 MVP 收斂為可維護產品的完整過程。

在 Lawsnote，我將 CPU 密集分析從約 100 秒才有結果改善到 5 秒內回傳首批資料，建立 MinIO events 與 Elasticsearch 資料缺漏監控，並處理 LLM 法規文字結構化與政府資料更新流程。在 MediaTek 專案中，我將資料倉儲頁面由 60 秒改善到 1 秒、模擬資料處理由 2 小時縮短至 1 分鐘。

我也直接參與 Apache Kafka upstream，處理 offset commit correctness、JVM container resource detection，以及 Kafka Streams bounded buffering。這些工作要求我理解大型系統在 production 的實際行為，並以測試、profiling 與 metrics 驗證修改。我雖不是純 Python 職涯背景，但能以既有的後端、資料平台與多語言經驗快速投入 Python 服務。

期待有機會進一步了解產品與團隊，也分享我如何把 AI prototype 建成可長期維運的後端系統。

Eric（Chen-Chien）Chang
