# MaiAgent — AI Agent Engineer 求職信

您好：

我想應徵 AI Agent Engineer。我對這個職位的興趣，來自它不只要求串接模型，也需要獨立開發 AI 產品與 API，並對第三方工具、架構與長期維護方式做出判斷。

我目前獨立開發開源專案 Locus：一個讓 AI agents 能共同使用不同領域、不同供應商真實資料的 context federation layer。Locus 的核心不是單一 API 或單一 domain，而是以 provider-neutral contract 將語意不同的來源聯邦成共同 context，同時保留 freshness、provenance、confidence、coverage gaps、partial failures 與 conflicting observations。第一個 reference flow 已將停車空位與相容 EV 充電樁狀態放進同一個決策流程，讓 agent 在跨來源資料過期、缺漏或衝突時仍產生有依據且可解釋的結果；服務透過 MCP 暴露並部署至 Cloudflare Workers / R2。

我曾在 Lawsnote 開發 LLM 法規文字結構化服務、政府資料更新流程與資料缺漏監控，也有 PostgreSQL、Elasticsearch、Docker、Linux 與 production backend 經驗。我同時直接參與 Apache Kafka upstream，處理 correctness、container runtime 與 bounded buffering，習慣用可重現測試與 profiling 驗證系統行為。

我的主要專業背景並非三年以上純 Python 開發，但具備 Python 使用經驗與跨 JavaScript、Go、Java、Rust 系統的實作能力。我會誠實面對框架差距，同時相信自己能快速補齊 Django、Celery 或 LLM framework 的使用方式，並把更難取代的資料 contract、分散式系統與產品交付能力帶進團隊。

謝謝您撥空閱讀，期待有機會進一步分享 Locus 與相關工程經驗。

Eric（Chen-Chien）Chang
